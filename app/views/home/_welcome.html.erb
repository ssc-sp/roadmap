<% title _("#{ApplicationService.application_name}") %>
<div>
  <div class="alert alert-danger">
    <%= sanitize _("<p>Proof of concept warnings, currently the tool is:</p>") %>
    <ul>
      <%= sanitize _("<li>only for <strong>unprotected data</strong></li> <li>testing and development</li>") %>
    </ul>
    <p><%= sanitize _("Link to comparison form:") %> <a href="https://forms.gle/whKoMacZeTPVA13G9">https://forms.gle/whKoMacZeTPVA13G9</a></p>
  </div>
  <h1><%= sanitize _("Welcome to %{application_name}") % {
    application_name: ApplicationService.application_name
  } %></h1>
  <p>
    <%= sanitize _('<p>%{application_name} has been deployed by the <strong>Data Solutions for Science</strong> team at <strong>Shared Services Canada</strong> for evaluation. The tool is intended to help you write data management plans.</p>') % {
      application_name: ApplicationService.application_name,
      organisation_name: Rails.configuration.x.organisation.name}
      %>

    <label for="linklist">
      <%=_('Getting started:')%>
    </label>
    <ul id="linklist">
      <% Rails.configuration.x.application.welcome_links.each do |wlink| %>
      <li><a href="<%= wlink[:url] %>"><%= wlink[:title] %></a></li>
      <% end %>
    </ul>
  </p>
</div>
